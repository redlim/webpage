import {en} from './en'
import {es} from './es'

export const languages = {
    en: 'English',
    es: 'Español',
};

export const defaultLang = 'es';

export const ui = {
    en,
    es,
} as const;